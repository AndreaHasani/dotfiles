# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#
# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Desktop Config
set $mod Mod1
set $super = Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:inconsolata 14 bold

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

### Variable

# Set Mode Names
set $resize_mode Resize Mode
set $quick_mode Quick Mode
set $move_mode Move Mode
set $screenshot_mode (c) Current Container | (f) Full Screen | (r) Region

#Declare Colors Variable

#for Statusbar
set $bg-color 	         #000000
set $inactive-bg-color   #000000
set $text-color          #f3f4f5
set $inactive-text-color #676E7D
set $urgent-bg-color     #a6c10d
set $urgent-fg-color     #000000

#Set Script Derecotry
set $script /home/strixx/Scripts/i3-scripts

# Window Color
# class                 border  backgr. text    indicator child_border
client.focused          #ff0000 #ff0000 #ffffff #0077ff   #ff0000
client.focused_inactive #ff0000 #5f676a #ffffff #484e50
client.unfocused        #ff0000 #222222 #888888 #000000   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #0077ff #0c0c0c #ffffff #0077ff   #0077ff

# Workspace Variables
set $ws1 "1:Desktop"
set $ws2 "2:Terminal"
set $ws3 "3:Browser"
set $ws4 "4:WebWork"
set $ws5 "5:Docker"
set $ws8 "8:Chat"
set $ws9 "9:File Manager"

#i3-gaps
gaps inner 10
gaps outer 5

# Specific Workspace Gapps
workspace $ws3 gaps inner 0
workspace $ws3 gaps outer 0

#Hide Borders and Title
for_window [class="^.*"] border pixel 1

# Enable smart border
smart_borders on

# Bar
bar {
    status_command py3status -c ~/.i3/i3status.conf
    height 30
    font pango:inconsolata 12, Awesome 10
    separator_symbol " "
    hidden_state hide
    modifier $mod
    strip_workspace_numbers no
    colors {

	separator #757575
	#                  border             background         text
	focused_workspace  $bg-color          $bg-color          $text-color
	inactive_workspace $inactive-bg-color $inactive-bg-color $inactive-text-color
	urgent_workspace   $urgent-bg-color   $urgent-bg-color   $urgent-fg-color
}
}

# Disable Focus Follow Mouse
focus_follows_mouse no

### Start Roles

# Role
for_window [window_role="pop-up"] floating enable
for_window [window_role="bubble"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_role="Preferences"] floating enable

# Type
for_window [window_type="dialog"] floating enable
for_window [window_type="menu"] floating enable

# Title
for_window [title="Set password for your browser"] floating enable
for_window [title="mutt-specified"] move container to workspace 6:Email
for_window [title="nvim_default"] move container to workspace 4:NVIM
for_window [title="Transmission"] floating enable. resize set 400 400, move position center

# Class
for_window [class="Oblogout"] floating enable
for_window [class="Firefox"] move container to workspace 3:Browser
for_window [class = "Firefox"] layout tabbed
for_window [class = "Polkit-gnome-authentication-agent-1"] focus, sticky enable
for_window [class = "feh"] floating enable, resize set 500 500, move position center

### End Roles

### Starting Modes

# Setting up modes

bindsym Pause mode "Normal Mode"


mode "Normal Mode" {

    # start a terminal
    bindsym $mod+Return exec /usr/bin/termite

    # kill focused window
    bindsym $mod+Shift+q kill

    # Scrachpad Settings
    bindsym $mod+minus move scratchpad
    bindsym $mod+plus scratchpad show

    # Rename Workspace With Dialog
    bindsym $mod+Shift+g exec ~/Scripts/i3-scripts/i3-toggle
    bindsym $mod+Shift+n exec i3-input -F \
    'rename workspace to "'$(i3-msg -t get_workspaces | jq '.[] | select(.focused==true).num')':%s"' \
    -P 'Rename workspace to '$(i3-msg -t get_workspaces | jq '.[] | select(.focused==true).num')': '

    # change focus
    bindsym $mod+h focus left
    bindsym $mod+j focus down
    bindsym $mod+k focus up
    bindsym $mod+l focus right

    # alternatively, you can use the cursor keys:
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right

    # split in horizontal orientation
    bindsym $mod+Shift+v split h

    # split in vertical orientation
    bindsym $mod+v split v

    # Scrachpad Settings
    bindsym $mod+minus move scratchpad
    bindsym $mod+plus exec --no-startup-id "python $script/i3-scripts/i3-scratchpad-rofi.py"

    # Drop down terminal
    bindsym $mod+Shift+t [title="dropdown_terminal"] scratchpad show
    # toggle tiling / floating
    bindsym $mod+Shift+space floating toggle

    # restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
    bindsym $mod+Shift+r restart

    # Exec oblogout
    bindsym $mod+Shift+e exec oblogout

    # Hide Bar
    bindsym $mod+b bar mode toggle

    ## God Mode Commands
    bindsym $mod+f fullscreen toggle
    bindsym Super_L exec --no-startup-id rofi -show drun  -display-drun 'App: ' -window-format '{n}' -lines 10 -fake-transparency -theme ~/Scripts/Rofi-Scripts/Rofi.rasi
    bindsym Super_R exec --no-startup-id rofi -show drun  -display-drun 'App: ' -window-format '{n}' -lines 10 -fake-transparency -theme ~/Scripts/Rofi-Scripts/Rofi.rasi

    # switch to workspace
    bindsym $mod+1 workspace $ws1 ; exec $script/get-workspacename
    bindsym $mod+2 workspace $ws2 ; exec $script/get-workspacename
    bindsym $mod+3 workspace $ws3 ; exec $script/get-workspacename
    bindsym $mod+4 workspace $ws4 ; exec $script/get-workspacename
    bindsym $mod+5 workspace $ws5 ; exec $script/get-workspacename
    bindsym $mod+6 workspace $ws6 ; exec $script/get-workspacename
    bindsym $mod+7 workspace number 7 ; exec $script/get-workspacename
    bindsym $mod+8 workspace $ws8 ; exec $script/get-workspacename
    bindsym $mod+9 workspace $ws9 ; exec $script/get-workspacename

    # Move
    bindsym $mod+Shift+h move left 25px
    bindsym $mod+Shift+j move down 25px
    bindsym $mod+Shift+k move up 25px
    bindsym $mod+Shift+l move right 25px

    # Other Modes
    bindsym $mod+r mode "$resize_mode"
    bindsym $mod+m mode "$move_mode"
    bindsym $mod+q mode "$quick_mode"
    bindsym $mod+Delete mode "$screenshot_mode"

}

mode "$Resize_mode" {

    # Resize
    bindsym h resize shrink width 15 px or 5 ppt
    bindsym j resize grow height 15 px or 5 ppt
    bindsym k resize shrink height 15 px or 5 ppt
    bindsym l resize grow width 15 px or 5 ppt

    bindsym Left resize shrink width 15 px or 5 ppt
    bindsym Down resize grow height 15 px or 5 ppt
    bindsym Up resize shrink height 15 px or 5 ppt
    bindsym Right resize grow width 15 px or 5 ppt

    # Escape Mode
    bindsym Escape mode "Normal Mode"
    bindsym n mode "Normal Mode"
    bindsym q mode "$quick_mode"
    bindsym m mode "$move_mode"

}

mode "$move_mode" {

    # Move
    bindsym h move left 25px
    bindsym j move down 25px
    bindsym k move up 25px
    bindsym l move right 25px

    # Move Center
    bindsym space move position center

    # Escape Mode
    bindsym Escape mode "Normal Mode"
    bindsym n mode "Normal Mode"
    bindsym q mode "$quick_mode"
    bindsym r mode "$Resize_mode"
}

mode "$screenshot_mode" {

    # Screenshot
    bindsym c exec maim -i $(xdotool getactivewindow) ~/Pictures/screenshots/Container\ $(date '+%Y-%m-%d_%H:%M').png, mode "Normal Mode"
    bindsym r exec maim -s ~/Pictures/screenshots/Region\ $(date '+%Y-%m-%d_%H:%M').png, mode "Normal Mode"
    bindsym f exec maim ~/Pictures/screenshots/Fullscreen\ $(date '+%Y-%m-%d_%H:%M').png, mode "Normal Mode"

    # Exit Mode
    bindsym Escape mode "Normal Mode"

}

mode "$quick_mode" {

    # move focused container to workspace
    bindsym 1 move container to workspace $ws1
    bindsym 2 move container to workspace $ws2
    bindsym 3 move container to workspace $ws3
    bindsym 4 move container to workspace $ws4
    bindsym 5 move container to workspace $ws5
    bindsym 6 move container to workspace $ws6
    bindsym 7 move container to workspace number 7
    bindsym 8 move container to workspace $ws8
    bindsym 9 move container to workspace $ws9
    bindsym 0 move container to workspace number 10

    # change focus
    bindsym h focus left
    bindsym j focus down
    bindsym k focus up
    bindsym l focus right

    # alternatively, you can use the cursor keys:
    bindsym Left focus left
    bindsym Down focus down
    bindsym Up focus up
    bindsym Right focus right

    # enter fullscreen mode for the focused container
    bindsym f fullscreen toggle

    # change container layout (stacked, tabbed, toggle split)
    bindsym w layout tabbed
    bindsym e layout toggle split

    # # change focus between tiling / floating windows
    bindsym $mod+space floating toggle
    bindsym space focus mode_toggle

    # focus the parent container
    bindsym p focus parent

    #Kill
    bindsym $mod+q kill

    # Sticky Window
    bindsym s sticky toggle ; exec notify-send.sh --replace-file=/tmp/workspacename Sticky -a 'Toggle' -t 1000

    # Escape Mod

    bindsym Escape mode "Normal Mode"
    bindsym n mode "Normal Mode"
    bindsym m mode "$move_mode"
    bindsym r mode "$Resize_mode"
}

### End Modes

### Start Exec

# Making sure the default mode changes to Normal Mode
exec_always --no-startup-id sleep 0.1 && xdotool key 127

## Startup Daemouns
exec --no-startup-id dunst &
exec --no-startup-id mpd &
exec --no-startup-id compton -b & syncthing -no-browser &
exec --no-startup-id redshift &
exec --no-startup-id sleep 1 && $script/Wallpaper-Change/execute
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &
exec --no-startup-id i3lock -eli ~/.i3lock/background.png &

## Startup Icons
exec --no-startup-id nm-applet &
exec --no-startup-id transmission-gtk -m &
exec --no-startup-id volumeicon &


### End Exec
